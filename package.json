{
  "name": "goalbuddy",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "dev": "npx lerna run dev",
    "start:frontend": "npx lerna run dev --scope @goalbuddy/frontend",
    "start:backend": "npx lerna run dev --scope @goalbuddy/backend"
  }
}
